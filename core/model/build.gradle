plugins {
    alias(libs.plugins.moviedatabase.jvm.library)
    alias(libs.plugins.kotlin.kover)
}

dependencies {
}

kover {
    currentProject {
        createVariant("merged") {
            add(["jvm"], false)
        }
    }
}
