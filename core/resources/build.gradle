plugins {
    alias(libs.plugins.moviedatabase.android.library)
    alias(libs.plugins.kotlin.kover)
}

android {
    namespace "io.viewpoint.moviedatabase.resources"
}

dependencies {
    implementation libs.androidx.material
}

kover {
    currentProject {
        createVariant("merged") {
            add(["debug"], false)
        }
    }
}
