<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/bottom_navigation"
    app:startDestination="@id/home_menu">

    <bottom
        android:id="@+id/home_menu"
        android:name="io.viewpoint.moviedatabase.ui.home.HomeFragment"
        android:label="HomeFragment"
        tools:layout="@layout/fragment_home" />
    <bottom
        android:id="@+id/movie_search_menu"
        android:name="io.viewpoint.moviedatabase.ui.search.MovieSearchFragment"
        android:label="MovieSearchFragment"
        tools:layout="@layout/item_search_result" />
    <bottom
        android:id="@+id/setting_menu"
        android:name="io.viewpoint.moviedatabase.ui.setting.SettingFragment"
        android:label="SettingFragment"
        tools:layout="@layout/fragment_setting" />
</navigation>